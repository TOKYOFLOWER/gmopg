# GMO-PG 契約・管理画面セットアップ手順

リンクタイプPlus を使うための GMO-PG 側の設定手順。**ユーザー（とみぃさん）の手作業**です。

---

## 0. 前提

- すでに GMO-PG マルチペイメントサービスを契約済み（リンクタイプを使っていた前提）
- 旧リンクタイプの契約から自動的に「リンクタイプPlus」が使えるようになっている可能性が高い
- 不明な場合は GMO-PG サポートに「リンクタイプPlus を使いたい」と問い合わせ

---

## 1. リンクタイプPlus 利用開始の確認

### 1-1. テスト管理画面ログイン

URL: https://stg.mul-pay.jp

ログインID とパスワードは契約時に GMO から発行されているはず。

### 1-2. 「リンクタイプPlus」メニューの存在確認

ログイン後、左メニュー or 上部メニューに「リンクタイプPlus」または「決済URL生成」の項目があるか確認。

**ない場合**: GMO サポート（営業日 9:00-19:00）に電話/フォームで「リンクタイプPlus の利用設定をお願いします」と連絡。

問い合わせフォーム: https://contact.gmo-pg.com/m?f=791

---

## 2. ConfigID の作成

ConfigID は「決済画面の見た目・動作の設定セット」を指す ID。複数作って使い分けることもできる。最低1つ作る。

### 2-1. テスト管理画面 → リンクタイプPlus → 設定

「設定の追加」を選択。

### 2-2. 設定値

以下の推奨値を入力：

| 項目 | 推奨値 | 備考 |
|---|---|---|
| 設定ID（ConfigID） | `default01` 等の任意の文字列 | 半角英数。後で GAS のスクリプトプロパティに登録 |
| 設定名 | 銀座東京フラワー 標準 | 管理用の表示名 |
| 表示言語 | 日本語 | |
| 利用通貨 | 円 (JPY) | |
| デザインパターン | 任意（4パターン×8色から選択） | 銀座東京フラワーの色味なら「ピンク系」「赤系」 |
| 決済有効期限 | デフォルト 7日（168時間） | 後で発行時に上書き可能 |
| 利用決済手段 | 契約済みのものを全選択 | クレカ／コンビニ／PayPay／楽天ペイ等 |
| 3Dセキュア | **2.0必須** | クレカ決済に必須 |
| セキュリティコード | **必須** | |

### 2-3. 結果通知URLの設定

**ここがとても重要**。後で Phase 3 で GAS WebApp の URL が確定したら登録する。

形式:
```
https://script.google.com/macros/s/AKfyc...XXX/exec?action=notify
```

### 2-4. 保存

設定が保存されたら、ConfigID をメモ。

---

## 3. ShopID と ShopPass の確認

### 3-1. テスト環境

管理画面 → アカウント情報 → ショップ情報 を開く。

- **ショップID**: `tshop00000000` 形式（先頭に `t`）
- **ショップパスワード**: 8〜16桁の文字列

ShopPass はマスクされているので、表示には別画面/権限が必要な場合あり。**初回設定時に GMO から書面で送られた値**を使うか、管理画面でリセット発行する。

### 3-2. 本番環境

URL: https://mul-pay.jp

同じ手順で取得。本番の ShopID は先頭が `shop`（`t` なし）。

---

## 4. 仕様書（Webマニュアル）へのアクセス

GMO の正式な API 仕様書は加盟店専用サイトに置かれている。

URL: https://docs.mul-pay.jp/linkplus/overview

ID/PW はマイページ → サポート → ドキュメント、または GMO に問い合わせて取得。

**特に確認すべきページ**:
- 「決済URL取得API」(`GetLinkplusUrlPayment.json`) のリクエスト/レスポンス仕様
- 「結果通知」のパラメータ一覧
- エラーコード一覧

---

## 5. テスト環境の擬似カード番号

テスト環境では以下のカード番号で動作確認できる：

| カード番号 | ブランド | 用途 |
|---|---|---|
| `4111111111111111` | VISA | 3DS非対応・正常決済 |
| `4000000000000002` | VISA | 3DS2.0対応・正常決済 |
| `4111111111111119` | VISA | 限度額超過エラー |
| `4111111111111127` | VISA | カード会社判定NG |
| `5555555555554444` | Master | 正常決済 |

- 有効期限: 任意の **未来** の月（例: `12/30`）
- CVC: 任意の3桁（例: `123`）
- 名義: 半角ローマ字（例: `TARO YAMADA`）

**重要**: これらは **テスト環境専用**。本番環境では使えない。本番テストは少額の実カード決済 → 即キャンセルで行う。

---

## 6. 完了チェックリスト

Claude Code に渡す前に、以下を全て満たしていることを確認：

- [ ] テスト管理画面 (https://stg.mul-pay.jp) にログインできる
- [ ] 本番管理画面 (https://mul-pay.jp) にログインできる
- [ ] テスト環境で ConfigID を1つ作成済み
- [ ] 本番環境で ConfigID を1つ作成済み
- [ ] テスト環境の ShopID と ShopPass を控えてある
- [ ] 本番環境の ShopID と ShopPass を控えてある
- [ ] 仕様書サイトにログインできる（任意だが推奨）

---

## 7. 取得した情報のメモ欄

実際の値はここに書かず、**1Password 等の安全な場所**に保管してから GAS のスクリプトプロパティに直接入力する。

このファイルは Git にコミットされる可能性があるため、**実値は絶対に書かないこと**。

```
[テスト環境]
GMO_TEST_SHOP_ID    = tshop________
GMO_TEST_SHOP_PASS  = (1Passwordから)
GMO_TEST_CONFIG_ID  = default01

[本番環境]
GMO_PROD_SHOP_ID    = shop________
GMO_PROD_SHOP_PASS  = (1Passwordから)
GMO_PROD_CONFIG_ID  = default01
```

---

## 8. トラブル時の問い合わせ先

- GMO-PG サポート: https://contact.gmo-pg.com/m?f=791
- マルペイ FAQ: https://mp-faq.gmo-pg.com/
- 営業時間: 平日 9:00-19:00

「リンクタイプPlus の API 設定について」と件名に書くとスムーズ。
